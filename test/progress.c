#include "progress.h"
#include <stdint.h>

// Stub implementations that do nothing for testing
void add_progress(uint16_t weight) BANKED {
    // Stub - do nothing in tests
}

void show_progress_page(char* title, char* message)  BANKED{
    // Stub - do nothing in tests
}
